#!/usr/bin/env ruby
$:.unshift File.expand_path('../../lib', __FILE__)

require 'wukong'

wu_file   = ARGV.shift
load wu_file 
processor = File.basename(wu_file, '.rb').to_sym

Wukong::LocalRunner.run(processor)
