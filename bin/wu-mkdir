#!/usr/bin/env ruby
require_relative '../lib/wukong/script/hadoop_runner'

Settings.resolve!

cmd = ['fs','-mkdir'] + Settings.rest

_, _, exit_status = Wukong::Script::HadoopRunner.run(cmd, mirror_io: true)
exit exit_status
